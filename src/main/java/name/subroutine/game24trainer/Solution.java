package name.subroutine.game24trainer;

public class Solution
{
    /**
     * postfix expression of the solution
     */
    public Symbol[] expression;

    /**
     * the only solution must be in the form of (a ? b) ? (c ? d)
     */
    public int twoByTwo = Puzzle.UNKNOWN;
}
